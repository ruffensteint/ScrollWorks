[package]
name = "scroll_core"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"
description = "ScrollWorks geometry: backbones, grown sweeps, acanthus leaves, joins and layering. No dependencies."

[dependencies]

[package]
name = "scrollworks"
version = "0.2.0"
edition = "2021"
license = "GPL-3.0-only"
description = "ScrollWorks: procedural acanthus scroll patterns for carving (native desktop app)."

[dependencies]
scroll_core = { path = "../core" }
eframe = "0.29.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rfd = "0.15"
